import 'package:flutter/material.dart';

/// App color palette extracted from the Mihrab design
class AppColors {
  AppColors._();

  // ══════════════════════════════════════════════
  //              LIGHT THEME COLORS
  // ══════════════════════════════════════════════

  /// Primary dark green — headers, borders, accents
  static const Color primaryDarkGreen = Color(0xFF2D4A2D);

  /// Medium green — selected states
  static const Color mediumGreen = Color(0xFF4A7C59);

  /// Teal/logo green
  static const Color tealGreen = Color(0xFF0D6E6E);

  /// Light olive/cream — backgrounds
  static const Color lightOlive = Color(0xFFD4C9A8);

  /// Warm cream — card backgrounds
  static const Color warmCream = Color(0xFFE8E0C8);

  /// Gold/amber — accent, selected indicators
  static const Color goldAmber = Color(0xFFC17B2A);

  /// Sand — secondary backgrounds
  static const Color sand = Color(0xFFBEB89A);

  /// Dark text
  static const Color darkText = Color(0xFF2D3A2D);

  /// White cream — lightest background
  static const Color whiteCream = Color(0xFFF0EBD8);

  // ══════════════════════════════════════════════
  //              DARK THEME COLORS
  // ══════════════════════════════════════════════

  /// Dark background
  static const Color darkBackground = Color(0xFF1A1A1A);

  /// Dark surface
  static const Color darkSurface = Color(0xFF2A2A2A);

  /// Dark cards
  static const Color darkCard = Color(0xFF333333);

  /// Warm white text for dark mode
  static const Color warmWhiteText = Color(0xFFE0D8C0);
}
