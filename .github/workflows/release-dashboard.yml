name: Build & Release APK (Dashboard)

on:
  workflow_dispatch:
  push:
    paths:
      - 'mihrab_dashboard/**'
      - 'mihrab_shared/**'
    branches: [main, master]

jobs:
  build:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable

      - name: Get dependencies (shared + dashboard)
        run: |
          cd mihrab_shared && flutter pub get && cd ..
          cd mihrab_dashboard && flutter pub get

      - name: Build APK
        run: cd mihrab_dashboard && flutter build apk --release

      - name: Get version
        id: version
        run: |
          VERSION=$(grep 'version:' mihrab_dashboard/pubspec.yaml | head -1 | sed 's/.*version: //' | sed 's/[[:space:]]//g')
          echo "version=$VERSION" >> $GITHUB_OUTPUT

      - name: Delete latest release (if exists)
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release delete latest --yes --cleanup-tag 2>/dev/null || true

      - name: Create Release & Upload APK
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release create latest \
            --title "Dashboard v${{ steps.version.outputs.version }} (Build #${{ github.run_number }})" \
            --notes "Automated build from [commit ${{ github.sha }}](https://github.com/${{ github.repository }}/commit/${{ github.sha }})" \
            --prerelease \
            mihrab_dashboard/build/app/outputs/flutter-apk/app-release.apk
